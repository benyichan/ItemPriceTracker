import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.itempricetracker',
  appName: 'ItemPriceTracker',
  webDir: 'dist'
};

export default config;
